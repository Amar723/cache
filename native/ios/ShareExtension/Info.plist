<?xml version="1.0" encoding="UTF-8"?>
<!--
  Info.plist for the iOS "ShareExtension" target.

  The predicate activation rule below makes Cache appear in the iOS share sheet
  whenever any shared attachment is a URL or text. The dictionary form is too
  strict for shares that include a URL plus preview media.
-->
<plist version="1.0">
<dict>
  <key>NSExtension</key>
  <dict>
    <key>NSExtensionAttributes</key>
    <dict>
      <key>NSExtensionActivationRule</key>
      <string>SUBQUERY (
        extensionItems,
        $extensionItem,
        SUBQUERY (
          $extensionItem.attachments,
          $attachment,
          ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "public.url"
          || ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "public.text"
        ).@count &gt;= 1
      ).@count &gt;= 1</string>
    </dict>
    <key>NSExtensionMainStoryboard</key>
    <string>MainInterface</string>
    <key>NSExtensionPointIdentifier</key>
    <string>com.apple.share-services</string>
  </dict>
</dict>
</plist>
